{% extends 'baseback2.html.twig' %}


{% block title %}Evenements{% endblock %}
{% block body %}


    <h1>Evenement</h1>

    <table class="table">
        <tbody>

            <tr>
                <th>Name Event</th>
                <td>{{ evenement.nomEvent }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ evenement.description }}</td>
            </tr>
            <tr>
                <th>Start Date</th>
                <td>{{ evenement.dateDebut }}</td>
            </tr>
            <tr>
                <th>End Date</th>
                <td>{{ evenement.dateFin }}</td>
            </tr>
            <tr>
                <th>Number of reservation</th>
                <td>{{ evenement.nbrReservation }}</td>
            </tr>
            <tr>
                <th>Price</th>
                <td>{{ evenement.prix }}</td>
            </tr>
            <tr>
                <th>Activities</th>
                <td>{{ evenement.activite }}</td>
            </tr>
        </tbody>
    </table>
    <div class="wrapper2">
<a href="{{ path('app_evenement_index') }}"><button data-bs-dismiss="modal" type="submit"
                                                        class="btn btn-primary">Back to list</button></a>

    <a href="{{ path('app_evenement_edit', {'idEvent': evenement.idEvent}) }}"><button data-bs-dismiss="modal" type="submit"
                                                                                       class="btn btn-primary">Edit</button></a>
 {{ include('evenement/_delete_form.html.twig') }}
    </div>
    {% endblock %}
