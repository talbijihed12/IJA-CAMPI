<form method="post" action="{{ path('app_participement_event_delete', {'idParticipement': participement_event.idParticipement}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ participement_event.idParticipement) }}">
    <button data-bs-dismiss="modal" type="submit"
            class="btn btn-primary">Delete </button>
</form>
