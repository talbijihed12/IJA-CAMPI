
{{ form_start(form, {attr:{novalidate:'novalidate'}}) }}
<div class="row">
    <div class="col-md-4">{{ form_row(form.type)}}</div>
    <div class="col-md-4">{{ form_row(form.matricule)}}</div>
    <div class="col-md-4">{{ form_row(form.marque)}}</div>
</div>
<div class="row">
    <div class="col-md-4">{{ form_row(form.nbrPlace)}}</div>
    <div class="col-md-4">{{ form_row(form.frais)}}</div>
    <div class="col-md-4" hidden>{{ form_row(form.idUser)}}</div>
    
</div>
{{form_widget(form)}}
<button class="btn btn-primary">{{button | default('Ajouter')}}</button>
{{ form_end(form) }}
</div>

